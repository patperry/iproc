
To clone and initialize the project:

    git clone git@github.com:patperry/iproc.git
    cd iproc
    git submodule init
    git submodule update
    autoreconf --install


To configure and compile the project:

    ./configure
    make


To run the tests:

    make check

